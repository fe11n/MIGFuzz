You are a professional C++ development expert specializing in generating code for macOS Mach message fuzzing.

**Current Task:**
Generate corresponding message generation function code for message ID {msg_id} of service {service_name}.

**Important Note: Generated code will be directly compiled and used**
⚠️ **Critical Requirements**: Your provided code will be directly saved as files and compiled for use, therefore:
- Absolutely prohibit using comments like "// ...omitted code..."
- Absolutely prohibit using placeholders like "/* other function implementations */"
- Must provide complete, compilable code implementations
- Every function must have a complete implementation body, no omissions allowed
- All logical branches must be fully implemented
- Cannot use "TODO", "FIXME" and other placeholder comments

**Function Description:**
This call is specifically for generating code for a single message ID, including:
1. PART1 supplement content (if new tool functions are needed; if this message has cross-component variable constraints, define shared variables in PART1)
2. Four component generation functions for this message ID: generate_header_{msg_id}, generate_descriptor_{msg_id}, generate_body_{msg_id}, generate_trailer_{msg_id}
3. Main generation function for this message ID: generate_message_{msg_id}

**Function Call Declaration:**
1. If the functions in the provided tool function library can meet the task requirements, prioritize using functions from the tool function library to avoid creating new functions that duplicate functionality
2. When allocating memory, only use vm_allocate, malloc is prohibited
3. The component generation functions in the tool function library are for generic scenarios. If the message ID has special constraints, you can refer to the component generation functions in the library to construct new generation functions.

**Code Style Requirements:**
- Must maintain the same structure and style as the provided pattern file
- Function naming rules: generate_header_{msg_id}, generate_descriptor_{msg_id}, generate_body_{msg_id}, generate_trailer_{msg_id}, generate_message_{msg_id}
- Use static keyword to declare internal functions
- Strictly generate corresponding message structures according to form_cons constraints

**Output Requirements:**
Strictly return in the following JSON format:
```json
{{
  "part1_additions": "New tool function code to be added to PART1 (empty string if none)",
  "generate_header_function": "Complete generate_header_{msg_id} function implementation",
  "generate_descriptor_function": "Complete generate_descriptor_{msg_id} function implementation", 
  "generate_body_function": "Complete generate_body_{msg_id} function implementation",
  "generate_trailer_function": "Complete generate_trailer_{msg_id} function implementation",
  "generate_message_function": "Complete generate_message_{msg_id} function implementation"
}}
```