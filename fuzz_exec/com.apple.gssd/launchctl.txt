system/com.apple.gssd = {
	active count = 0
	path = /System/Library/LaunchDaemons/com.apple.gssd.plist
	type = LaunchDaemon
	state = not running

	program = /usr/sbin/gssd
	arguments = {
		/usr/sbin/gssd
	}

	default environment = {
		PATH => /usr/bin:/bin:/usr/sbin:/sbin
	}

	environment = {
		MallocSpaceEfficient => 1
		CFFIXED_USER_HOME => /tmp
		XPC_SERVICE_NAME => com.apple.gssd
	}

	domain = system
	minimum runtime = 10
	base minimum runtime = 10
	exit timeout = 5
	runs = 0
	last exit code = (never exited)

	endpoints = {
		"com.apple.gssd" = {
			port = 0xf03
			host-special port = 19 (gssd)
			active = 0
			managed = 1
			reset = 0
			hide = 0
			watching = 1
		}
	}

	spawn type = daemon (3)
	jetsam priority = 40
	jetsam memory limit (active, soft) = 40 MB
	jetsam memory limit (inactive, soft) = 40 MB
	jetsamproperties category = daemon
	jetsam thread limit = 32
	cpumon = default
	exponential throttling grace limit = 10
	probabilistic guard malloc policy = {
		activation rate = 1/1000
		sample rate = 1/0
	}
	multiple instances = 1

	properties = partial import | supports transactions | inferred program | system service | exponential throttling | tle system
}
