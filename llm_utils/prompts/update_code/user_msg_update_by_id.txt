请基于以下输入信息，生成改进后的 Mach 消息构造代码，使其满足消息处理函数的约束并通过验证。

【服务信息】
- 服务名称：{service_name}
- 消息 ID：{message_id}

【工具函数库说明】
以下是生成的代码可用的工具函数库说明文件：
```
{generate_tools}
```

【消息处理函数约束（cons）】
以下内容由处理函数源码总结，可能存在误差，请结合源码交叉验证：
```
{message_constraints}
```

【现有消息生成代码（generate_code）】
包含 PART1 (头文件与工具函数) 片段和该消息 ID 对应的各组件生成函数：
```
{original_generate_code}
```
注意，该函数片段生成的消息未能满足消息验证函数的验证需求。需要你根据得到的信息分析原因，对其进行修改，使其生成的消息能够通过消息验证函数。

【历史信息 (History Results)】
这里记录了最初的faillog、之前所有大模型的更新代码（回答）以及验证结果。
如果大模型更新后的代码能够通过编译，验证结果将是一份新的 failtrace (即该代码check失败)；
如果大模型更新后的代码连编译都没通过，验证结果将是直接的编译报错（即该代码编译失败）。
请你在分析时务必参考这些内容，避免重复之前大模型犯过的错误。
```
{history_result}
```

【当前代码的 failtrace 调试信息】
以下为上述【现有消息生成代码（generate_code）】执行所对应的 failtrace 日志（或针对该代码的初始/最新编译报错）。
请你明确针对该 faillog 对当前代码进行更新。分析本次 failtrace 的执行路径或报错信息，思考导致当前代码执行失败的原因，并提供针对性的修改方案。
```
{failtrace}
```


【任务要求】
1. 结合 handle_func 源码、cons 与 failtrace，明确当前消息在哪个字段或结构未满足约束，形成了什么格式问题。
2. 找到 generate_code 中对应生成逻辑的具体位置，说明需要如何调整以满足处理函数的期望。
3. 按要求返回新的消息生成代码。所有生成的函数需保证 C++14 语法正确并可直接编译。

【输出 JSON 结构】
返回单个 JSON 对象，字段说明如下：
- "part1_content": 字符串；完整的 PART1 内容（包含头文件引用和工具函数定义）。如果需要修改 PART1（如添加新工具函数或头文件），请返回修改后的完整内容；如果无需修改，请不输出此字段。注意：不需要包含 PART1 的介绍性注释（如 "// PART1: Header file inclusions..."），直接从 #include 开始即可。
- "generate_header": 字符串；完整的 generate_header_{message_id} 实现，若无需改动请不输出此字段。
- "generate_descriptor": 字符串；完整的 generate_descriptor_{message_id} 实现，若无需改动请不输出此字段。
- "generate_body": 字符串；完整的 generate_body_{message_id} 实现，若无需改动请不输出此字段。
- "generate_trailer": 字符串；完整的 generate_trailer_{message_id} 实现，若无需改动请不输出此字段。
- "generate_messag": 字符串；完整的 generate_message_{message_id} 实现，若无需改动请不输出此字段。
- "notes": 字符串；总结你对问题定位与修改方案的分析，需覆盖任务要求 1 和 2。

注意：
- 只返回符合上述结构的 JSON 文本，不要输出额外文字。
- 当引用 PART1 工具函数时，请确认相应函数已存在或在 "part1_content" 中包含其实现。
- 生成的代码将被直接编译和使用，绝不能包含任何省略和占位符。