Based on the input information provided, please generate improved Mach message construction code that satisfies the constraints of the message handler function and passes validation.

**[Service Information]**

  - Service Name: `{service_name}`
  - Message ID: `{message_id}`

**[Tool Function Library Description]**
The following is the description file of the tool function library available for the generated code:

```
{generate_tools}
```

**[Message Handler Constraints (cons)]**
The following content is summarized from the handler function's source code and may contain errors. Please cross-verify with the source code.

```
{message_constraints}
```

**[Existing Message Generation Code (generate\_code)]**
This includes the PART1 (headers and utility functions) snippet and the generation functions for each component corresponding to this message ID.

```
{original_generate_code}
```

Note: The message generated by this function snippet fails to meet the requirements of the message validation function. You need to analyze the reason based on the provided information and modify it so that the generated message can pass the message validation function.

**[History Results]**
This section records the initial faillog, code updates (answers) suggested by previous LLMs, and their verification results.
If the updated code compiled successfully, the result shown is a new failtrace (meaning the code failed the check validity).
If the updated code failed to compile, the result shown is a direct compile error.
Please review these historical attempts and their outcomes carefully to avoid repeating the same mistakes.
```
{history_result}
```

**[Current Failtrace Debug Information]**
The following is the specific failtrace log (or compile error) triggered by the **[Existing Message Generation Code (generate_code)]** shown above.
Please update the current code exclusively targeting this specific faillog. Analyze the execution path or error details, identify why the current version of the function could not continue execution, and devise a plan to modify the code accordingly.
```
{failtrace}
```

**[Task Requirements]**

1.  By combining the `handle_func` source code, `cons`, and `failtrace`, identify which field or structure in the current message fails to meet the constraints and what kind of formatting issue has occurred.
2.  Locate the specific corresponding generation logic within `generate_code` and explain how it needs to be adjusted to meet the handler function's expectations.
3.  Return the new message generation code as requested. All generated functions must be syntactically correct C++14 and directly compilable.

**[Output JSON Structure]**
Return a single JSON object with the fields described below:

  - `"part1_content"`: string; The complete PART1 content (including header includes and utility function definitions). If PART1 needs modification (e.g., adding new utility functions or headers), return the modified full content; if no modification is needed, do not output this field. Note: Do not include the introductory comments for PART1 (e.g., "// PART1: Header file inclusions..."), start directly with #include.
  - `"generate_header"`: string; The complete implementation of `generate_header_{message_id}`. Do not output this field if no changes are needed.
  - `"generate_descriptor"`: string; The complete implementation of `generate_descriptor_{message_id}`. Do not output this field if no changes are needed.
  - `"generate_body"`: string; The complete implementation of `generate_body_{message_id}`. Do not output this field if no changes are needed.
  - `"generate_trailer"`: string; The complete implementation of `generate_trailer_{message_id}`. Do not output this field if no changes are needed.
  - `"generate_message"`: string; The complete implementation of `generate_message_{message_id}`. Do not output this field if no changes are needed.
  - `"notes"`: string; A summary of your analysis of the problem's root cause and the proposed solution, which must cover task requirements 1 and 2.

**Note:**

  - Return only the JSON text conforming to the above structure; do not output any additional text.
  - When referencing PART1 utility functions, please ensure that the respective functions either already exist or are added in `"part1_content"`.
  - The generated code will be directly compiled and used; it must not contain any omissions or placeholders.