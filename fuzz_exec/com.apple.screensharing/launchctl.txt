system/com.apple.screensharing = {
	active count = 0
	path = /System/Library/LaunchDaemons/com.apple.screensharing.plist
	type = LaunchDaemon
	state = not running

	program = /System/Library/CoreServices/RemoteManagement/screensharingd.bundle/Contents/MacOS/screensharingd
	arguments = {
		/System/Library/CoreServices/RemoteManagement/screensharingd.bundle/Contents/MacOS/screensharingd
	}

	default environment = {
		PATH => /usr/bin:/bin:/usr/sbin:/sbin
	}

	environment = {
		MallocSpaceEfficient => 1
		XPC_SERVICE_NAME => com.apple.screensharing
	}

	domain = system
	minimum runtime = 10
	base minimum runtime = 10
	exit timeout = 5
	runs = 0
	last exit code = (never exited)

	event triggers = {
		Listener => {
			keepalive = 0
			service = com.apple.screensharing
			stream = com.apple.bonjour.registration
			monitor = com.apple.UserEventAgent-System
			descriptor = {
				"Bonjour" => "rfb"
				"SockServiceName" => "vnc-server"
			}
		}
	}

	endpoints = {
		"com.apple.screensharing.server" = {
			port = 0x0
			active = 0
			managed = 1
			reset = 1
			hide = 1
			watching = 1
		}
	}

	event channels = {
		"com.apple.bonjour.registration" = {
			port = 0x0
			active = 0
			managed = 1
			reset = 0
			hide = 0
			watching = 1
		}
	}

	sockets = {
		"Listener" = {
			type = stream
			service name = vnc-server

			sockets = {
				19 (no bytes to read)
				20 (no bytes to read)
			}

			active = 0
			passive = 1
			bonjour = 1
			ipv4v6 = 0
			receive_packet_info = 0
		}
	}

	spawn type = interactive (4)
	jetsam priority = 40
	jetsam memory limit (active, soft) = 1000 MB
	jetsam memory limit (inactive, soft) = 1000 MB
	jetsamproperties category = daemon
	jetsam thread limit = 32
	cpumon = default
	exponential throttling grace limit = 10
	probabilistic guard malloc policy = {
		activation rate = 1/1000
		sample rate = 1/0
	}

	properties = partial import | supports transactions | inferred program | system service | exponential throttling | tle system
}
