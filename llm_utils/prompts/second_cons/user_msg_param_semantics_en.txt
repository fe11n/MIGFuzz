You are analysing service {service_name}. Use the data below to infer the call parameters and decide how each aligns with existing semantic types.

### Existing aggregated parameter types
```json
{current_parameter_types}
```

### Message pseudocode to evaluate
```json
{message_payload}
```

Follow the system instructions to derive the `parameters` array. Reply with JSON only; do not include Markdown or additional commentary.
