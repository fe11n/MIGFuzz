system/com.apple.filesystems.fskitd = {
	active count = 0
	path = /System/Library/LaunchDaemons/com.apple.filesystems.fskitd.plist
	type = LaunchDaemon
	state = not running

	program = /usr/libexec/fskitd
	arguments = {
		/usr/libexec/fskitd
	}

	default environment = {
		PATH => /usr/bin:/bin:/usr/sbin:/sbin
	}

	environment = {
		MallocSpaceEfficient => 1
		XPC_SERVICE_NAME => com.apple.filesystems.fskitd
	}

	domain = system
	minimum runtime = 10
	base minimum runtime = 10
	exit timeout = 5
	runs = 1
	last exit code = 0

	endpoints = {
		"com.apple.filesystems.fskitd" = {
			port = 0x11a03
			active = 0
			managed = 1
			reset = 0
			hide = 0
			watching = 1
		}
	}

	spawn type = interactive (4)
	jetsam priority = 180
	jetsam memory limit (active, soft) = 25 MB
	jetsam memory limit (inactive, soft) = 15 MB
	jetsamproperties category = daemon
	jetsam thread limit = 32
	cpumon = default
	exponential throttling grace limit = 10
	probabilistic guard malloc policy = {
		activation rate = 1/1000
		sample rate = 1/0
	}

	properties = supports transactions | supports pressured exit | inferred program | system service | exponential throttling | tle system
}
